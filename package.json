{
  "name": "usagetap-model-lifecycle-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action example that checks discovered AI model keys against UsageTap.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  }
}
